### Ascii Ocean TypeScript Webapp

New portfolio webapp.

- Primary Wave: y₁ = A·sin(kx - ωt + φ) 
- Inverse Wave: y₂ = -A·sin(kx - ωt + φ + π)
- Composite: y_total = y₁ + y₂ + interference_term

```bash
    yarn install
    yarn dev
```
